[tool.poetry]
name = "mpc_planner"
version = "0.1.0"
description = ""
authors = ["oscardegroot <groot.oscar.de@gmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = ">=3.10,<3.13"
casadi = ">=3.6.0"
numpy = ">=1.21.0,<2.0"
scipy = ">=1.8"
requests = ">=2.5"
pyyaml = "^6.0.1"
matplotlib = ">=3.6,<4.0"
pytest-cov = "^4.1.0"
black = "^24.2.0"
acados-template = {path = "/workspace/acados/interfaces/acados_template", develop = true}

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
